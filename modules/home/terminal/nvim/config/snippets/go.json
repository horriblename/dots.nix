{
    "for loop map": {
        "prefix": ["forMap"],
        "body": [
            "${1:result} := make([]${2:resultType}, 0, len(${3:input}))",
            "for _, x := range ${3} {",
            "	${1} = append(${1}, ${2})",
            "}",
            ""
        ]
    }
}
