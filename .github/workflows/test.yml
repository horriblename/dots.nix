name: "Test"
on:
  push:
    branches:
      - main
    paths:
      - "flake.lock"
  schedule:
    - cron: "0 0 * * 5"
jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: cachix/install-nix-action@v20
        with:
          github_access_token: ${{ secrets.GITHUB_TOKEN }}
      - uses: cachix/cachix-action@v12
        with:
          name: horriblename
          authToken: "${{ screts.CACHIX_AUTH_TOKEN }}"
      - run: "nix build .#ghActionsBuilder"
      - run: "nix path-info -r .#ghActionsBuilder | cachix push horriblename"
